// TODO: Ajouter un uploader ici, avec gestion des erreurs et des retours d'état (progression, succès, échec) pour les fichiers uploadés.
// L'uploader doit être générique et réutilisable dans différentes parties de l'application (ex: upload d'avatar, upload de documents, etc.)
// Il doit également permettre de configurer les types de fichiers acceptés, la taille maximale, et les callbacks pour les événements d'upload (onSuccess, onError, onProgress).
// Possibilité de preview + brancher avec les méthods dans `@/utils/request/fetch.ts` pour envoyer les fichiers en AJAX au backend.